apiVersion: apps/v1
kind: Deployment
metadata:
	name: myapp
spec:
	replicas: 2
	selector:
		matchlabels:
			app: myapp
	template:
		metadata:
			labels:
				app: myapp	
		spec:

			contaniners:
			- name: myapp
			  image: rounakj06/myapp:latest
			  ports:
			  - containerPort: 80